<?php

/*
 * Generated by CRUDigniter v3.0 Beta 
 * www.crudigniter.com
 */

class Masters_carat_model extends CI_Model {

    function __construct() {
        parent::__construct();
    }

    /*
     * Get masters_carat by CARAT_ID
     */

    function get_masters_carat($CARAT_ID) {
        return $this->db->get_where('masters_carat', array('CARAT_ID' => $CARAT_ID))->row_array();
    }

    /*
     * Get all masters_carat
     */

    function get_all_masters_carat() {
        return $this->db->get('masters_carat')->result_array();
    }

    /**
     * Function to get all masters carat data
     * 
     * @param type $request
     * @param type $user_data
     * 
     * return array result
     */
    function getAllMastersCaratData($request, $user_data = array()) {
        try {
            $sql_details = array('user' => $this->db->username, 'pass' => $this->db->password, 'db' => $this->db->database, 'host' => $this->db->hostname);
            $request['searchcolumns'] = array();

            $columns = array(
                array(
                    'db' => 'CARAT_ID',
                    'dt' => 'DT_RowId',
                    'formatter' => function( $d, $row ) {
                        return 'row_' . $d;
                    }
                ),
                array('db' => 'CARAT_ID',
                    'dt' => 0,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'CARAT_NAME',
                    'dt' => 1,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'CARAT_ID',
                    'dt' => 2,
                    'formatter' => function($d, $row) {
                        $returnString = "";
                        $returnString .= '<a href="' . site_url('masters_carat/edit/' . $row['CARAT_ID']) . '" class="btn btn-info btn-xs"><span class="fa fa-pencil"></span> Edit</a><br>';
                        $returnString .= '<a href="' . site_url('masters_carat/remove/' . $row['CARAT_ID']) . '" class="btn btn-danger btn-xs"><span class="fa fa-trash"></span> Delete</a>';
                        return $returnString;
                    }
                )
            );

            $join = "";
            $where = "";

            $query_columns_array = array("CARAT_ID, CARAT_NAME, CLIENT_ID, IS_ACTIVE");

            $custom_where = array();
            $where .= " WHERE CREATED_BY = " . $_SESSION['user']['LOGIN_ID'] . " AND IS_ACTIVE = 1";
            $custom_where_string = (count($custom_where) > 0) ? implode(" AND ", array_unique($custom_where)) : "";
            $request['custom_where'] = $custom_where_string;
            $query_columns = implode(",", array_unique($query_columns_array));
            $sql_query = 'SELECT $query_columns from masters_carat' . $join . $where;
            $result = datatable::simple($request, $sql_details, $sql_query, $query_columns, $columns);
            $start = $_REQUEST['start'];
            return $result;
        } catch (Exception $e) {
            log_message("Error : ", $this->db->_error_message());
            return fasle;
        }
    }

    /*
     * function to add new masters_carat
     */

    function add_masters_carat($params) {
        $this->db->insert('masters_carat', $params);
        return $this->db->insert_id();
    }

    /*
     * function to update masters_carat
     */

    function update_masters_carat($CARAT_ID, $params) {
        $this->db->where('CARAT_ID', $CARAT_ID);
        $response = $this->db->update('masters_carat', $params);
        if ($response) {
            return "masters_carat updated successfully";
        } else {
            return "Error occuring while updating masters_carat";
        }
    }

    /*
     * function to delete masters_carat
     */

    function delete_masters_carat($CARAT_ID) {
        $response = $this->db->delete('masters_carat', array('CARAT_ID' => $CARAT_ID));
        if ($response) {
            return "masters_carat deleted successfully";
        } else {
            return "Error occuring while deleting masters_carat";
        }
    }

}
