<?php

/*
 * Generated by CRUDigniter v3.0 Beta 
 * www.crudigniter.com
 */

class Masters_ornament_desc_model extends CI_Model {

    function __construct() {
        parent::__construct();
    }

    /**
     * Function to get all masters document data
     * 
     * @param type $request
     * @param type $user_data
     * 
     * @return type
     */
    function getAllMastersOrnamentDescData($request, $user_data) {
        try {
            $sql_details = array('user' => $this->db->username, 'pass' => $this->db->password, 'db' => $this->db->database, 'host' => $this->db->hostname);
            $request['searchcolumns'] = array();

            $columns = array(
                array(
                    'db' => 'ORNAMENT_DESC_ID',
                    'dt' => 'DT_RowId',
                    'formatter' => function( $d, $row ) {
                        return 'row_' . $d;
                    }
                ),
                array('db' => 'ORNAMENT_DESC_ID',
                    'dt' => 0,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'ORNAMENT_DESC_NAME',
                    'dt' => 1,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'ORNAMENT_DESCRIPTION',
                    'dt' => 2,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'ORNAMENT_TYPE',
                    'dt' => 3,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'ORNAMENT_DESC_ID',
                    'dt' => 4,
                    'formatter' => function($d, $row) {
                        $returnString = "";
                        $returnString .= '<a href="' . site_url('masters_ornament_desc/edit/' . $row['ORNAMENT_DESC_ID']) . '" class="btn btn-info btn-xs"><span class="fa fa-pencil"></span> Edit</a><br>';
                        $returnString .= '<a href="' . site_url('masters_ornament_desc/remove/' . $row['ORNAMENT_DESC_ID']) . '" class="btn btn-danger btn-xs"><span class="fa fa-trash"></span> Delete</a>';
                        return $returnString;
                    }
                )
            );

            $join = "";
            $where = "";

            $query_columns_array = array("ORNAMENT_DESC_ID, ORNAMENT_DESC_NAME, ORNAMENT_DESCRIPTION, ORNAMENT_TYPE");

            $custom_where = array();
            $where .= " WHERE CREATED_BY = " . $_SESSION['user']['LOGIN_ID'] . " AND   IS_ACTIVE = 1";
            $custom_where_string = (count($custom_where) > 0) ? implode(" AND ", array_unique($custom_where)) : "";
            $request['custom_where'] = $custom_where_string;
            $query_columns = implode(",", array_unique($query_columns_array));
            $sql_query = 'SELECT $query_columns from masters_ornament_desc' . $join . $where;
            $result = datatable::simple($request, $sql_details, $sql_query, $query_columns, $columns);
            $start = $_REQUEST['start'];
            return $result;
        } catch (Exception $e) {
            log_message("Error : ", $this->db->_error_message());
            return fasle;
        }
    }

    /*
     * Get masters_ornament_desc by ORNAMENT_DESC_ID
     */

    function get_masters_ornament_desc($ORNAMENT_DESC_ID) {
        return $this->db->get_where('masters_ornament_desc', array('ORNAMENT_DESC_ID' => $ORNAMENT_DESC_ID))->row_array();
    }

    /*
     * Get all masters_ornament_desc
     */

    function get_all_masters_ornament_desc() {
        return $this->db->get('masters_ornament_desc')->result_array();
    }

    /*
     * function to add new masters_ornament_desc
     */

    function add_masters_ornament_desc($params) {
        $this->db->insert('masters_ornament_desc', $params);
        return $this->db->insert_id();
    }

    /*
     * function to update masters_ornament_desc
     */

    function update_masters_ornament_desc($ORNAMENT_DESC_ID, $params) {
        $this->db->where('ORNAMENT_DESC_ID', $ORNAMENT_DESC_ID);
        $response = $this->db->update('masters_ornament_desc', $params);
        if ($response) {
            return "masters_ornament_desc updated successfully";
        } else {
            return "Error occuring while updating masters_ornament_desc";
        }
    }

    /*
     * function to delete masters_ornament_desc
     */

    function delete_masters_ornament_desc($ORNAMENT_DESC_ID) {
        $response = $this->db->delete('masters_ornament_desc', array('ORNAMENT_DESC_ID' => $ORNAMENT_DESC_ID));
        if ($response) {
            return "masters_ornament_desc deleted successfully";
        } else {
            return "Error occuring while deleting masters_ornament_desc";
        }
    }

}
