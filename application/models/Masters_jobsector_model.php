<?php

/*
 * Generated by CRUDigniter v3.0 Beta 
 * www.crudigniter.com
 */

class Masters_jobsector_model extends CI_Model {

    function __construct() {
        parent::__construct();
    }

    /*
     * Get masters_jobsector by JOBSECTOR_ID
     */

    function get_masters_jobsector($JOBSECTOR_ID) {
        return $this->db->get_where('masters_jobsector', array('JOBSECTOR_ID' => $JOBSECTOR_ID))->row_array();
    }

    /**
     * Function to get all masters document data
     * 
     * @param type $request
     * @param type $user_data
     * 
     * @return type
     */
    function getAllMastersJobSectorData($request, $user_data) {
        try {
            $sql_details = array('user' => $this->db->username, 'pass' => $this->db->password, 'db' => $this->db->database, 'host' => $this->db->hostname);
            $request['searchcolumns'] = array();

            $columns = array(
                array(
                    'db' => 'JOBSECTOR_ID',
                    'dt' => 'DT_RowId',
                    'formatter' => function( $d, $row ) {
                        return 'row_' . $d;
                    }
                ),
                array('db' => 'JOBSECTOR_ID',
                    'dt' => 0,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'JOBSECTOR_NAME',
                    'dt' => 1,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'LOAN_TYPE',
                    'dt' => 2,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'JOBSECTOR_ID',
                    'dt' => 3,
                    'formatter' => function($d, $row) {
                        $returnString = "";
                        $returnString .= '<a href="' . site_url('masters_jobsector/edit/' . $row['JOBSECTOR_ID']) . '" class="btn btn-info btn-xs"><span class="fa fa-pencil"></span> Edit</a><br>';
                        $returnString .= '<a href="' . site_url('masters_jobsector/remove/' . $row['JOBSECTOR_ID']) . '" class="btn btn-danger btn-xs"><span class="fa fa-trash"></span> Delete</a>';
                        return $returnString;
                    }
                )
            );

            $join = "";
            $where = "";

            $query_columns_array = array("JOBSECTOR_ID, JOBSECTOR_NAME, LOAN_TYPE");

            $custom_where = array();
            $where .= " WHERE CREATED_BY = " . $_SESSION['user']['LOGIN_ID'] . " AND   IS_ACTIVE = 1";
            $custom_where_string = (count($custom_where) > 0) ? implode(" AND ", array_unique($custom_where)) : "";
            $request['custom_where'] = $custom_where_string;
            $query_columns = implode(",", array_unique($query_columns_array));
            $sql_query = 'SELECT $query_columns from masters_jobsector' . $join . $where;
            $result = datatable::simple($request, $sql_details, $sql_query, $query_columns, $columns);
            $start = $_REQUEST['start'];
            return $result;
        } catch (Exception $e) {
            log_message("Error : ", $this->db->_error_message());
            return fasle;
        }
    }

    /*
     * Get all masters_jobsector
     */

    function get_all_masters_jobsector() {
        return $this->db->get('masters_jobsector')->result_array();
    }

    /*
     * function to add new masters_jobsector
     */

    function add_masters_jobsector($params) {
        $this->db->insert('masters_jobsector', $params);
        return $this->db->insert_id();
    }

    /*
     * function to update masters_jobsector
     */

    function update_masters_jobsector($JOBSECTOR_ID, $params) {
        $this->db->where('JOBSECTOR_ID', $JOBSECTOR_ID);
        $response = $this->db->update('masters_jobsector', $params);
        if ($response) {
            return "masters_jobsector updated successfully";
        } else {
            return "Error occuring while updating masters_jobsector";
        }
    }

    /*
     * function to delete masters_jobsector
     */

    function delete_masters_jobsector($JOBSECTOR_ID) {
        $response = $this->db->delete('masters_jobsector', array('JOBSECTOR_ID' => $JOBSECTOR_ID));
        if ($response) {
            return "masters_jobsector deleted successfully";
        } else {
            return "Error occuring while deleting masters_jobsector";
        }
    }

}
