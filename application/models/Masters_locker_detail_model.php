<?php

/*
 * Generated by CRUDigniter v3.0 Beta 
 * www.crudigniter.com
 */

class Masters_locker_detail_model extends CI_Model {

    function __construct() {
        parent::__construct();
    }

    /**
     * Function to get all masters document data
     * 
     * @param type $request
     * @param type $user_data
     * 
     * @return type
     */
    function getAllMastersLockerDetailsData($request, $user_data) {
        try {
            $sql_details = array('user' => $this->db->username, 'pass' => $this->db->password, 'db' => $this->db->database, 'host' => $this->db->hostname);
            $request['searchcolumns'] = array();

            $columns = array(
                array(
                    'db' => 'DETAIL_ID',
                    'dt' => 'DT_RowId',
                    'formatter' => function( $d, $row ) {
                        return 'row_' . $d;
                    }
                ),
                array('db' => 'DETAIL_ID',
                    'dt' => 0,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'LOCKER_ID',
                    'dt' => 1,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'DETAIL_ID',
                    'dt' => 2,
                    'formatter' => function($d, $row) {
                        $returnString = "";
                        $returnString .= '<a href="' . site_url('masters_locker_detail/edit/' . $row['DETAIL_ID']) . '" class="btn btn-info btn-xs"><span class="fa fa-pencil"></span> Edit</a><br>';
                        $returnString .= '<a href="' . site_url('masters_locker_detail/remove/' . $row['DETAIL_ID']) . '" class="btn btn-danger btn-xs"><span class="fa fa-trash"></span> Delete</a>';
                        return $returnString;
                    }
                )
            );

            $join = "";
            $where = "";

            $query_columns_array = array("DETAIL_ID, LOCKER_ID");

            $custom_where = array();
            $where .= " WHERE CREATED_BY = " . $_SESSION['user']['LOGIN_ID'] . " AND   IS_ACTIVE = 1";
            $custom_where_string = (count($custom_where) > 0) ? implode(" AND ", array_unique($custom_where)) : "";
            $request['custom_where'] = $custom_where_string;
            $query_columns = implode(",", array_unique($query_columns_array));
            $sql_query = 'SELECT $query_columns from masters_locker_details' . $join . $where;
            $result = datatable::simple($request, $sql_details, $sql_query, $query_columns, $columns);
            $start = $_REQUEST['start'];
            return $result;
        } catch (Exception $e) {
            log_message("Error : ", $this->db->_error_message());
            return fasle;
        }
    }

    /*
     * Get masters_locker_detail by DETAIL_ID
     */

    function get_masters_locker_detail($DETAIL_ID) {
        return $this->db->get_where('masters_locker_details', array('DETAIL_ID' => $DETAIL_ID))->row_array();
    }

    /*
     * Get all masters_locker_details
     */

    function get_all_masters_locker_details() {
        return $this->db->get('masters_locker_details')->result_array();
    }

    /*
     * function to add new masters_locker_detail
     */

    function add_masters_locker_detail($params) {
        $this->db->insert('masters_locker_details', $params);
        return $this->db->insert_id();
    }

    /*
     * function to update masters_locker_detail
     */

    function update_masters_locker_detail($DETAIL_ID, $params) {
        $this->db->where('DETAIL_ID', $DETAIL_ID);
        $response = $this->db->update('masters_locker_details', $params);
        if ($response) {
            return "masters_locker_detail updated successfully";
        } else {
            return "Error occuring while updating masters_locker_detail";
        }
    }

    /*
     * function to delete masters_locker_detail
     */

    function delete_masters_locker_detail($DETAIL_ID) {
        $response = $this->db->delete('masters_locker_details', array('DETAIL_ID' => $DETAIL_ID));
        if ($response) {
            return "masters_locker_detail deleted successfully";
        } else {
            return "Error occuring while deleting masters_locker_detail";
        }
    }

}
