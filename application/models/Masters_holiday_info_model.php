<?php

/*
 * Generated by CRUDigniter v3.0 Beta 
 * www.crudigniter.com
 */

class Masters_holiday_info_model extends CI_Model {

    function __construct() {
        parent::__construct();
    }

    /*
     * Get masters_holiday_info by HOLIDAY_ID
     */

    function get_masters_holiday_info($HOLIDAY_ID) {
        return $this->db->get_where('masters_holiday_info', array('HOLIDAY_ID' => $HOLIDAY_ID))->row_array();
    }

    /*
     * Get all masters_holiday_info
     */

    function get_all_masters_holiday_info() {
        return $this->db->get('masters_holiday_info')->result_array();
    }

    /*
     * function to add new masters_holiday_info
     */

    function add_masters_holiday_info($params) {
        $this->db->insert('masters_holiday_info', $params);
        return $this->db->insert_id();
    }

    /**
     * Function to get all masters holiday info data
     * 
     * @param type $request
     * @param type $user_data
     * 
     * @return type
     */
    function getAllMastersHolidayInfoData($request, $user_data = array()) {
        try {
            $sql_details = array('user' => $this->db->username, 'pass' => $this->db->password, 'db' => $this->db->database, 'host' => $this->db->hostname);
            $request['searchcolumns'] = array();

            $columns = array(
                array(
                    'db' => 'HOLIDAY_ID',
                    'dt' => 'DT_RowId',
                    'formatter' => function( $d, $row ) {
                        return 'row_' . $d;
                    }
                ),
                array('db' => 'HOLIDAY_ID',
                    'dt' => 0,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'HOLIDAY_NAME',
                    'dt' => 1,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'HOLIDAY_FROM_DATE',
                    'dt' => 2,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'HOLIDAY_TO_DATE',
                    'dt' => 3,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'HOLIDAY_ID',
                    'dt' => 4,
                    'formatter' => function($d, $row) {
                        $returnString = "";
                        $returnString .= '<a href="' . site_url('masters_holiday_info/edit/' . $row['HOLIDAY_ID']) . '" class="btn btn-info btn-xs"><span class="fa fa-pencil"></span> Edit</a><br>';
                        $returnString .= '<a href="' . site_url('masters_holiday_info/remove/' . $row['HOLIDAY_ID']) . '" class="btn btn-danger btn-xs"><span class="fa fa-trash"></span> Delete</a>';
                        return $returnString;
                    }
                )
            );

            $join = "";
            $where = "";

            $query_columns_array = array("HOLIDAY_ID, HOLIDAY_NAME, HOLIDAY_FROM_DATE, HOLIDAY_TO_DATE");

            $custom_where = array();
            $where .= " WHERE CREATED_BY = " . $_SESSION['EMP_DATA']['LOGIN_ID'] . " AND   IS_ACTIVE = 1";
            $custom_where_string = (count($custom_where) > 0) ? implode(" AND ", array_unique($custom_where)) : "";
            $request['custom_where'] = $custom_where_string;
            $query_columns = implode(",", array_unique($query_columns_array));
            $sql_query = 'SELECT $query_columns from masters_holiday_info' . $join . $where;
            $result = datatable::simple($request, $sql_details, $sql_query, $query_columns, $columns);
            $start = $_REQUEST['start'];
            return $result;
        } catch (Exception $e) {
            log_message("Error : ", $this->db->_error_message());
            return fasle;
        }
    }

    /*
     * function to update masters_holiday_info
     */

    function update_masters_holiday_info($HOLIDAY_ID, $params) {
        $this->db->where('HOLIDAY_ID', $HOLIDAY_ID);
        $response = $this->db->update('masters_holiday_info', $params);
        if ($response) {
            return "masters_holiday_info updated successfully";
        } else {
            return "Error occuring while updating masters_holiday_info";
        }
    }

    /*
     * function to delete masters_holiday_info
     */

    function delete_masters_holiday_info($HOLIDAY_ID) {
        $response = $this->db->delete('masters_holiday_info', array('HOLIDAY_ID' => $HOLIDAY_ID));
        if ($response) {
            return "masters_holiday_info deleted successfully";
        } else {
            return "Error occuring while deleting masters_holiday_info";
        }
    }

}
