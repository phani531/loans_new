<?php

/*
 * Generated by CRUDigniter v3.0 Beta 
 * www.crudigniter.com
 */

class Masters_race_model extends CI_Model {

    function __construct() {
        parent::__construct();
    }

    /**
     * Function to get all masters document data
     * 
     * @param type $request
     * @param type $user_data
     * 
     * @return type
     */
    function getAllMastersRaceData($request, $user_data) {
        try {
            $sql_details = array('user' => $this->db->username, 'pass' => $this->db->password, 'db' => $this->db->database, 'host' => $this->db->hostname);
            $request['searchcolumns'] = array();

            $columns = array(
                array(
                    'db' => 'RACE_ID',
                    'dt' => 'DT_RowId',
                    'formatter' => function( $d, $row ) {
                        return 'row_' . $d;
                    }
                ),
                array('db' => 'RACE_ID',
                    'dt' => 0,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'RACE_NAME',
                    'dt' => 1,
                    'formatter' => function($d, $row) {
                        return !empty($d) ? $d : "N/A";
                    }
                ),
                array('db' => 'RACE_ID',
                    'dt' => 2,
                    'formatter' => function($d, $row) {
                        $returnString = "";
                        $returnString .= '<a href="' . site_url('masters_race/edit/' . $row['RACE_ID']) . '" class="btn btn-info btn-xs"><span class="fa fa-pencil"></span> Edit</a><br>';
                        $returnString .= '<a href="' . site_url('masters_race/remove/' . $row['RACE_ID']) . '" class="btn btn-danger btn-xs"><span class="fa fa-trash"></span> Delete</a>';
                        return $returnString;
                    }
                )
            );

            $join = "";
            $where = "";

            $query_columns_array = array("RACE_ID, RACE_NAME");

            $custom_where = array();
            $where .= " WHERE CREATED_BY = " . $_SESSION['user']['LOGIN_ID'] . " AND   IS_ACTIVE = 1";
            $custom_where_string = (count($custom_where) > 0) ? implode(" AND ", array_unique($custom_where)) : "";
            $request['custom_where'] = $custom_where_string;
            $query_columns = implode(",", array_unique($query_columns_array));
            $sql_query = 'SELECT $query_columns from masters_race' . $join . $where;
            $result = datatable::simple($request, $sql_details, $sql_query, $query_columns, $columns);
            $start = $_REQUEST['start'];
            return $result;
        } catch (Exception $e) {
            log_message("Error : ", $this->db->_error_message());
            return fasle;
        }
    }

    /*
     * Get masters_race by RACE_ID
     */

    function get_masters_race($RACE_ID) {
        return $this->db->get_where('masters_race', array('RACE_ID' => $RACE_ID))->row_array();
    }

    /*
     * Get all masters_race
     */

    function get_all_masters_race() {
        return $this->db->get('masters_race')->result_array();
    }

    /*
     * function to add new masters_race
     */

    function add_masters_race($params) {
        $this->db->insert('masters_race', $params);
        return $this->db->insert_id();
    }

    /*
     * function to update masters_race
     */

    function update_masters_race($RACE_ID, $params) {
        $this->db->where('RACE_ID', $RACE_ID);
        $response = $this->db->update('masters_race', $params);
        if ($response) {
            return "masters_race updated successfully";
        } else {
            return "Error occuring while updating masters_race";
        }
    }

    /*
     * function to delete masters_race
     */

    function delete_masters_race($RACE_ID) {
        $response = $this->db->delete('masters_race', array('RACE_ID' => $RACE_ID));
        if ($response) {
            return "masters_race deleted successfully";
        } else {
            return "Error occuring while deleting masters_race";
        }
    }

}
